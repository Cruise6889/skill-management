# Skill 拆解器（Skill Explorer）MVP PRD

- 版本：v0.1
- 状态：待开发
- 产品形态：本地优先的单用户桌面应用
- 建议实现：Electron + React/TypeScript + SQLite（无远程服务端）

## 1. 产品概述

Skill 拆解器帮助经常接触各类 Agent Skill 的用户，快速看懂一个陌生 Skill 的用途、触发方式、工作流程、约束和文件结构，并将其沉淀为可搜索、可整理的个人 Skill 资料库。

首版聚焦“理解与学习”，不把自己做成 Skill 市场、代码编辑器或完整的安装/发布平台。

## 2. 用户问题

目标用户日常会接触大量 Skill，常见困难包括：

- 导入一个陌生 Skill 后，不知道它适用于什么场景、应如何调用；
- `SKILL.md`、脚本、参考资料和资产分散在不同文件中，理解成本高；
- Skill 来源多（本地、GitHub、不同 Agent 生态），缺少统一的浏览和管理入口；
- 需要 AI 帮助理解复杂 Skill，但不希望默认上传本地文件或自动产生 API 费用；
- 需要保留个人分类、标签、收藏和检索记录，方便以后复用与学习。

## 3. 产品目标

### 3.1 首版目标

1. 用户可导入本机或 GitHub 中的 Skill，并在同一界面查看其文件结构与内容。
2. 系统可在不调用 AI 的前提下，基于规则生成基础拆解结果。
3. 用户可显式选择文件和模型后，发起 AI 深度拆解。
4. 用户可通过搜索、分类、标签和收藏，管理个人资料库中的 Skill。

### 3.2 成功标准

一名用户导入陌生的 Codex Skill 后，能在 3 分钟内回答：

- 这个 Skill 是为了解决什么问题；
- 什么时候应该或不应该调用它；
- 核心步骤、输入输出和限制是什么；
- 目录中每类文件分别负责什么。

### 3.3 非目标

首版不提供：

- 用户账户、云端同步、多人协作或 Skill 市场；
- 在线编辑 Skill 文件、执行脚本或运行安装命令；
- 自动 AI 分析、自动将内容上传到第三方模型；
- 全量 Git 历史、自动拉取更新、文件实时监听；
- 安装到 Codex/Claude/Cursor、冲突覆盖与回滚；
- 复杂的逐行 Diff、目录 Diff、引用关系图和自定义解析规则编辑器。

## 4. 目标用户与典型任务

### 4.1 目标用户

- 高频使用 Codex、Claude Code、Cursor 等 Agent Skill 的个人用户；
- 希望学习优质 Skill 设计方法的开发者、提示词工程师和知识工作者；
- 有本地 Skill 文件、GitHub 仓库或 ZIP 包需要整理的用户。

### 4.2 核心用户故事

1. 作为学习者，我想选择本机某个 Skill 文件夹，让系统自动展示目录与拆解结果，从而快速理解它。
2. 作为收藏者，我想输入 GitHub 仓库地址并导入完整仓库，从而研究其中一个或多个 Skill。
3. 作为资料库使用者，我想用搜索、标签、分类和收藏定位曾研究过的 Skill。
4. 作为谨慎的 AI 用户，我想先看到要上传的文件，再手动发起 AI 分析，避免误传敏感内容和不必要费用。

## 5. MVP 范围与优先级

| 优先级 | 功能 | MVP 是否实现 | 说明 |
| --- | --- | --- | --- |
| P0 | 本机目录导入 | 是 | 支持选择一个 Skill 目录并复制到资料库；原目录不被删除或修改。 |
| P0 | GitHub 仓库导入 | 是 | 输入公开仓库 URL，克隆完整仓库；不执行仓库脚本、Hook 或安装命令。 |
| P0 | Codex 规则解析 | 是 | 识别 `SKILL.md`、YAML frontmatter、常见目录和文件引用。 |
| P0 | 三栏详情页 | 是 | 目录树、文件预览、Skill 拆解结果。 |
| P0 | 资料库管理 | 是 | 搜索、分类、标签、收藏、从资料库移除。 |
| P0 | 手动 AI 深度分析 | 是 | 用户主动选择后调用；AI Key 由用户提供。 |
| P1 | ZIP 导入 | 否 | 下一版本支持。 |
| P1 | 关联原始目录 | 否 | MVP 仅复制入资料库，避免路径失效和同步复杂度。 |
| P1 | GitHub 更新检查 | 否 | 下一版本支持手动检查和拉取更新。 |
| P1 | Skill 对比 | 否 | 先做两个 Skill 的拆解结果并排比较。 |
| P2 | 安装、导出 | 否 | 安装前须有目标目录和冲突预览；不进入 MVP。 |
| P2 | 在线编辑、历史版本、复杂 Diff、引用图 | 否 | 作为后续专业能力。 |

## 6. 功能需求

### 6.1 导入 Skill

#### 本机目录导入

- 用户通过系统目录选择器选择一个文件夹。
- 系统先展示待导入目录、入口文件检测结果和文件数量，用户确认后导入。
- 文件被复制到应用资料库；资料库记录来源路径、导入时间、来源类型和内容摘要。
- 不修改、移动或删除用户的原始目录。
- 如果导入目录没有 `SKILL.md`，允许作为“自定义格式 Skill”导入，并提示仅提供通用展示与基础分析。

#### GitHub 仓库导入

- 用户输入公开 GitHub 仓库 URL。
- 系统克隆完整仓库到应用资料库，展示仓库名、默认分支和克隆状态。
- 解析仓库内的 Skill 候选目录；用户选择一个目录进入资料库。
- 禁止执行仓库中的 Shell 脚本、包管理器脚本、Git Hook 或安装流程。
- 克隆失败时展示可理解的错误：URL 无效、无网络、仓库不存在、权限不足或 Git 不可用。

### 6.2 规则拆解

规则解析在导入成功后自动执行，不调用第三方 AI。

对于 Codex 风格 Skill，系统应提取：

- 基本信息：名称、描述、路径、来源、格式类型、导入时间；
- 应用场景与核心功能：根据 `description` 和正文标题/段落抽取；
- 触发条件：显式调用方式、关键词和自动触发描述；
- 执行流程：按有序步骤或章节归纳；
- 输入与输出：从指令、脚本参数和模板中提取；
- 约束条件：禁止项、前置条件、权限、文件/网络依赖；
- 依赖工具：脚本、MCP、CLI、外部服务或运行时；
- 目录与文件职责：`SKILL.md`、`scripts/`、`references/`、`assets/` 等；
- 文件引用关系：正文中出现的相对文件路径与其目标文件。

解析结果必须标注“规则解析”，并允许用户看到每条结果对应的原始文件依据。

### 6.3 AI 深度分析

- 默认关闭，不因导入自动执行。
- 用户在详情页点击“AI 深度拆解”后，先进入确认页。
- 确认页列出将发送的文件、字符量估算、目标服务商、模型和预计请求量；用户可以取消勾选文件。
- 首版支持用户配置一个 OpenAI-compatible API 地址、API Key 和模型名称。
- AI 返回内容以结构化字段保存，并标注模型、分析时间、分析文件清单和提示词版本。
- AI 结果与规则解析结果并列展示；AI 不能覆盖规则解析得到的原始事实。
- API Key 存入操作系统密钥库，不写入 SQLite、日志或导出文件。

### 6.4 Skill 详情与文件预览

详情页采用三栏布局：

- 左栏：目录树、文件搜索、文件类型图标；
- 中栏：原始文件预览；
- 右栏：Skill 概览、规则解析和 AI 深度分析结果。

MVP 支持预览：Markdown、纯文本、JSON、YAML、常见代码文件和图片。暂不支持编辑。PDF、Office、音视频显示为文件信息卡片和“暂不预览”提示。

### 6.5 资料库管理

- 首页显示 Skill 卡片或列表：名称、来源、格式、标签、收藏状态、最近分析时间；
- 支持按名称、描述、路径和标签关键词搜索；
- 用户可创建分类、添加/删除标签、收藏/取消收藏；
- 支持按来源、格式、分类、收藏、是否完成 AI 分析筛选；
- “从资料库移除”只删除应用内副本和数据库索引，操作前二次确认；绝不删除原始本机目录或远程仓库。

## 7. 页面与交互

### 7.1 资料库首页

- 顶部：全局搜索、导入按钮、设置入口；
- 左侧：全部 Skill、收藏、分类、标签、来源筛选；
- 主区：Skill 列表/卡片、导入状态和空状态引导。

### 7.2 导入流程

1. 用户选择“本机目录”或“GitHub 仓库”。
2. 系统检测入口文件和文件数量。
3. 用户确认导入。
4. 系统复制/克隆、规则解析并显示进度。
5. 完成后进入该 Skill 的详情页。

### 7.3 Skill 详情页

- 顶部展示名称、来源、格式、标签、收藏和“AI 深度拆解”按钮；
- 三栏内容随选择的文件或分析类型更新；
- 规则解析为空或失败时，展示原因和可重试入口；
- AI 分析中可离开页面，完成后在资料库中更新状态。

## 8. 数据模型（概要）

### Skill

- `id`、`name`、`description`、`format`、`source_type`、`source_url`；
- `library_path`、`original_path`、`imported_at`、`updated_at`；
- `category_id`、`is_favorite`、`analysis_status`。

### File

- `id`、`skill_id`、`relative_path`、`type`、`size`、`content_hash`；
- `previewable`、`is_entry_file`。

### Analysis

- `id`、`skill_id`、`kind`（规则/AI）、`schema_version`、`content`；
- `source_files`、`model`、`prompt_version`、`created_at`、`status`。

### Taxonomy

- `categories`：用户自定义分类；
- `tags`：用户自定义标签；
- `skill_tags`：Skill 与标签的多对多关联。

## 9. 安全与隐私要求

- 默认本地存储，应用不上传任何 Skill 内容；
- 必须在用户确认后才调用 AI；
- 不执行任何导入 Skill 的脚本和可执行文件；
- GitHub 导入不得执行 Hook；
- 所有本地文件操作均限制在用户选择的目录与应用资料库中；
- 删除动作可恢复性优先：仅移除应用副本，原始文件永不受影响；
- 日志不得记录 API Key、完整敏感文件内容或用户绝对路径。

## 10. 非功能需求

- 支持 macOS 首发；架构不阻碍后续 Windows 支持；
- 本机导入包含 200 个文本文件、总计不超过 20MB 的 Skill 时，规则解析应在 10 秒内完成；
- 搜索 1,000 个资料库条目时，首个结果应在 1 秒内出现；
- 导入、克隆、解析和 AI 分析均需显示进度、成功或可操作的失败原因；
- 应用在离线状态下，除 GitHub 导入和 AI 分析外的既有资料库功能均可使用。

## 11. 验收标准

MVP 交付需满足：

1. 用户能导入一个包含 `SKILL.md`、`scripts/` 和 `references/` 的本机 Skill，且原目录内容未被修改。
2. 系统在详情页完整展示目录树，能预览 `SKILL.md`、YAML、JSON 和代码文件。
3. 系统能生成并展示 8 个拆解栏目：基本信息、应用场景与核心功能、触发条件、执行流程、输入输出、约束条件、依赖工具、目录与文件职责；存在引用时额外展示引用关系。
4. 用户能通过名称或标签检索 Skill，并能完成分类、标签、收藏和从资料库移除。
5. 用户未确认前，系统不会向 AI 服务发送任何内容；确认后，能记录所选模型和文件清单。
6. GitHub 仓库导入失败和 AI 请求失败均有明确、可行动的提示。

## 12. 后续版本路线

### v0.2

- ZIP 导入、关联原始目录、手动 Git 更新；
- Claude Code、Cursor 规则适配；
- 两个 Skill 的拆解结果并排对比；
- PDF 与更多文件格式预览。

### v0.3

- 安装与导出；
- 安装冲突预览、覆盖/重命名/取消和备份；
- 文件变更检测、分析重新运行；
- Skill 在线编辑与版本历史。

### 公开产品阶段

- 跨平台安装包、自动更新；
- 可选账户和云端同步；
- 团队资料库、共享和公开 Skill 市场；
- 自定义解析规则与更多模型服务商。
