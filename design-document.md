# Skill 拆解器（Skill Explorer）MVP 产品设计文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 需求依据 | 本文档已归并 MVP 需求与设计决策 |
| 产品形态 | 本地优先的单用户桌面应用 |
| 首发平台 | macOS |
| 建议技术栈 | Electron + React + TypeScript + SQLite |
| 文档状态 | 待评审 |

## 1. 设计摘要

Skill 拆解器是一个面向 Agent Skill 学习者和收藏者的本地资料库。用户可以从本机目录或公开 GitHub 仓库导入 Skill，通过统一的文件树、原文预览和结构化拆解结果，理解 Skill 的用途、触发方式、执行流程、输入输出、约束及文件职责。

MVP 使用“规则拆解为默认、AI 深度分析为显式可选增强”的产品模式。应用不执行导入内容，不修改原始目录，不在用户确认前上传文件。

### 1.1 核心产品决策

1. **导入即复制**：本机 Skill 复制到应用资料库，后续操作仅作用于副本。
2. **规则先行**：导入完成后立即本地解析，即使离线、未配置 API Key 也能得到基础结果。
3. **AI 显式授权**：必须经过文件选择与二次确认后才发起请求。
4. **事实与推断分离**：规则结果保留文件依据；AI 结果单独展示，不覆盖规则事实。
5. **不执行不可信内容**：GitHub 导入仅克隆与读取，不执行 Hook、脚本、安装命令或可执行文件。

## 2. 产品目标与设计原则

### 2.1 用户目标

用户导入一个陌生 Codex Skill 后，应在 3 分钟内明确：

- Skill 解决的问题及适用场景；
- 应该与不应该调用的条件；
- 执行步骤、输入、输出与主要限制；
- 各目录和关键文件的职责。

### 2.2 设计原则

- **原文可追溯**：拆解结果可回到具体文件和原文位置。
- **渐进展开**：先呈现结论，再展开证据、相关文件与细节。
- **操作可预期**：导入、解析、AI 分析和移除均明示范围、进度和结果。
- **本地优先**：除 GitHub 导入和 AI 请求外，核心功能在离线下可用。
- **隐私默认严格**：不自动上传，密钥不进入数据库或日志，日志不记录完整文件内容。

### 2.3 MVP 边界

本版本不包含 ZIP 导入、目录实时同步、Git 更新、Skill 对比、安装、导出、在线编辑、历史版本、复杂 Diff、引用关系图、自定义解析规则、账户、云同步和多人协作。

## 3. 用户、任务与使用场景

### 3.1 主要用户

- 使用 Codex、Claude Code、Cursor 等 Agent 工具的个人用户；
- 学习 Skill 设计方法的开发者、提示词工程师和知识工作者；
- 需要整理本地目录或公开 GitHub Skill 仓库的收藏者。

### 3.2 核心任务

| 任务 | 用户输入 | 产品输出 | 完成标志 |
| --- | --- | --- | --- |
| 导入本机 Skill | 本机目录 | 库内副本、文件索引、规则结果 | 进入详情页且原目录未变 |
| 导入 GitHub Skill | 公开仓库 URL、候选目录 | 仓库副本、Skill 条目、规则结果 | 选定 Skill 可在资料库中浏览 |
| 理解 Skill | 文件/拆解栏目选择 | 原文预览、8 类拆解、证据 | 用户能回答用途、触发、流程与限制 |
| AI 深度拆解 | 模型配置、勾选文件、确认 | 结构化 AI 结果及分析元数据 | 结果与规则分析并列可见 |
| 管理资料库 | 搜索、筛选、分类、标签、收藏 | 匹配的 Skill 列表 | 1,000 条下 1 秒内出现首个结果 |

## 4. 信息架构与导航

```text
资料库首页
├─ 全部 Skill
├─ 收藏
├─ 分类
├─ 标签
├─ 来源筛选
├─ 导入
│  ├─ 本机目录导入
│  └─ GitHub 仓库导入
├─ Skill 详情
│  ├─ 目录树与文件搜索
│  ├─ 文件预览
│  ├─ 规则拆解
│  └─ AI 深度拆解
└─ 设置
   ├─ AI 服务配置
   ├─ 资料库位置与容量
   └─ 隐私与日志
```

### 4.1 全局导航

- 应用启动默认进入资料库首页。
- 顶部常驻全局搜索、“导入 Skill”主按钮和设置入口。
- 详情页保留返回前一列表状态的能力，包括搜索词、筛选条件和滚动位置。

## 5. 关键流程设计

### 5.1 本机目录导入

1. 用户点击“导入 Skill”，选择“本机目录”。
2. 系统目录选择器返回路径；渲染进程仅获得经脱敏的目录名和扫描结果。
3. 主进程进行只读预检：
   - 目录是否可读；
   - 文件数与总大小；
   - `SKILL.md` 是否存在；
   - 是否包含超大文件、符号链接或不可读项。
4. 确认页展示目录名、来源、入口检测、文件数、总大小和警告。
5. 用户确认后，系统生成 Skill ID，复制到临时目录，校验完成后原子移入资料库目录。
6. 系统建立文件索引并运行规则解析。
7. 导入完成后进入 Skill 详情页；无 `SKILL.md` 时显示“自定义格式”提示。

**复制默认策略**

- 不跟随指向源目录外的符号链接；在预检页列为警告并跳过。
- 跳过 `.git/`、系统隐藏元数据和应用临时文件；其他隐藏文件保留但不默认选入 AI 分析。
- 导入失败时清理未提交的临时副本和数据库临时记录。

### 5.2 GitHub 仓库导入

1. 用户输入 HTTPS 公开仓库 URL。
2. 系统规范化并校验 URL，仅允许 `https://github.com/{owner}/{repo}` 及其 `.git` 形式。
3. 检查 Git 可用性，使用禁用 Hook 的受控子进程执行浅克隆；不检出子模块，不运行 LFS 下载或仓库中任何命令。
4. 页面展示仓库名、默认分支、克隆进度和当前阶段。
5. 克隆后扫描候选目录：
   - 含 `SKILL.md` 的目录为高置信候选；
   - 含 `scripts/`、`references/` 或 `assets/` 且包含 Markdown 入口的目录为低置信候选。
6. 用户选择一个候选目录导入。一个仓库的其他候选 Skill 可稍后再次选择，MVP 每次建立一个 Skill 条目。
7. 系统建立索引、运行规则解析并进入详情页。

**错误映射**

| 技术错误 | 用户文案 | 建议操作 |
| --- | --- | --- |
| URL 格式不匹配 | 请输入公开 GitHub 仓库地址 | 修正地址 |
| DNS/连接超时 | 无法连接 GitHub | 检查网络后重试 |
| 404 | 仓库不存在或地址错误 | 在浏览器中确认地址 |
| 401/403 | 无法访问该仓库，MVP 仅支持公开仓库 | 改用公开仓库 |
| Git 不可用 | 未找到 Git | 安装 Git 或改用本机目录导入 |
| 无候选目录 | 未检测到 Skill 入口 | 返回或将仓库根目录作为自定义格式导入 |

### 5.3 规则拆解

1. 读取文件索引，确定格式和入口文件。
2. 解析 `SKILL.md` 的 YAML frontmatter、标题、段落、列表、代码块和路径文本。
3. 按置信度将内容映射到 8 个栏目，保留每条结果的证据。
4. 解析相对文件引用，标记已解析、缺失、越界或外部链接。
5. 事务式保存解析结果，更新 Skill 分析状态。
6. 如部分解析失败，保留已成功栏目，展示局部错误与“重新解析”入口。

### 5.4 AI 深度拆解

1. 用户点击“AI 深度拆解”。
2. 若尚未配置服务，进入 AI 设置，保存后返回确认页。
3. 确认页展示：
   - API 服务基础地址的主机名；
   - 模型名称；
   - 将发送的文件清单、单文件字符数和总字符数；
   - 预计请求批次，不承诺价格；
   - 密钥仅存储于系统密钥库的提示。
4. 默认勾选 `SKILL.md` 及其存在的相对引用文本文件；默认不勾选二进制文件、图片、隐藏文件、超限文件和疑似密钥文件。
5. 用户可取消勾选，但不能勾选不支持发送的文件。
6. 用户点击“确认并发送”后创建分析快照，记录文件清单及其内容哈希，再发起 API 请求。
7. 请求可在后台继续；资料库卡片和详情页同步展示状态。
8. 返回后先做结构校验，再保存结果。原始 API 响应不写入普通日志。

### 5.5 从资料库移除

1. 用户从详情页或列表菜单选择“从资料库移除”。
2. 二次确认弹窗明确说明将删除应用副本、索引、分析和标签关联，不会删除原始目录或 GitHub 仓库。
3. 确认后先将库内副本移到应用回收站，再软删除数据库记录。
4. MVP 提供当次会话内的“撤销”提示；应用回收站可按固定保留期清理，默认 7 天。

## 6. 页面与交互设计

### 6.1 资料库首页

**页面目标**：让用户快速找到、筛选或导入 Skill。

**布局**

- 顶部栏：全局搜索框、当前结果数、导入按钮、设置。
- 左侧导航：全部、收藏、分类、标签、来源。
- 主内容区：列表/卡片视图、筛选条件、排序、导入与分析状态。

**Skill 卡片信息层级**

1. 名称、收藏状态、格式标识；
2. 两行内的描述摘要；
3. 来源类型与脱敏后来源；
4. 分类与最多 3 个标签；
5. 规则解析状态、AI 分析状态及最近分析时间。

**搜索与筛选**

- 搜索字段：名称、描述、相对路径、标签名。
- 筛选维度：来源、格式、分类、收藏、AI 分析状态。
- 多个筛选维度之间取交集，同一维度的多选值取并集。
- 输入 150 ms 后触发搜索；支持键盘清空和筛选标签单项移除。

**状态**

- 首次空状态：解释产品价值，提供“导入本机目录”和“导入 GitHub 仓库”。
- 无搜索结果：展示当前条件，提供清除筛选。
- 进行中：导入或 AI 分析卡片展示进度和阶段，不阻断其他操作。

### 6.2 导入对话框

导入采用宽对话框，分为“选择来源 → 预检/克隆 → 确认或选择候选 → 导入与解析”。

进度拆分为：

- 正在检查来源；
- 正在复制或克隆；
- 正在建立文件索引；
- 正在解析 Skill；
- 完成。

如果无法得到精确百分比，展示不确定进度条和当前阶段，不显示虚假百分比。

### 6.3 Skill 详情页

**顶部摘要区**

- 返回按钮、Skill 名称、来源和格式；
- 收藏、分类、标签；
- “AI 深度拆解”主操作；
- 更多菜单：重新规则解析、在 Finder 中显示库内副本、从资料库移除。

**三栏布局**

| 区域 | 建议宽度 | 功能 |
| --- | --- | --- |
| 左栏 | 240–320 px，可拖拽 | 文件搜索、目录树、类型图标、引用/入口标记 |
| 中栏 | 剩余空间的 45%–60% | 文件预览、路径面包屑、文件信息 |
| 右栏 | 360–480 px，可拖拽 | 概览、规则拆解、AI 拆解、证据展开 |

- 小窗口下三栏切换为“文件 / 预览 / 拆解”页签，避免过度压缩。
- 点击解析依据时，中栏打开目标文件并定位到对应行。
- 文件树选择与拆解栏目选择相互独立，不因切换文件丢失右栏阅读位置。

### 6.4 文件预览

| 类型 | 处理方式 |
| --- | --- |
| Markdown | 默认渲染视图，可切换原文；禁止执行内嵌 HTML/脚本 |
| 纯文本、代码 | 等宽字体、行号、语法高亮、只读 |
| JSON/YAML | 原文为主，解析错误可提示但不阻断预览 |
| 图片 | 缩放、适应容器、文件信息 |
| PDF/Office/音视频 | 文件信息卡、格式、大小、“MVP 暂不预览” |
| 未知或二进制 | 仅文件信息，不尝试作为文本打开 |

默认不一次性将整个文件内容发送到渲染进程。大文件使用分块读取，超过预览上限时显示截断提示和文件信息。

### 6.5 右栏拆解信息

顶部使用两个主页签：“规则拆解”和“AI 深度拆解”。

规则拆解固定展示：

1. 基本信息；
2. 应用场景与核心功能；
3. 触发条件；
4. 执行流程；
5. 输入与输出；
6. 约束条件；
7. 依赖工具；
8. 目录与文件职责；
9. 文件引用关系（存在时显示）。

每条结果包含：摘要、解析标识、置信度和“查看依据”。置信度采用“高 / 中 / 低”，避免展示无法校准的精确百分比。

AI 页签在未分析时展示隐私说明与发起按钮；完成后展示结构化结果、模型、时间、所用文件、提示词版本和重新分析入口。

### 6.6 AI 设置

- API Base URL：必填，仅允许 HTTPS；本机开发可显式开启 localhost HTTP。
- API Key：必填，输入后仅显示已保存状态和末 4 位；值存入 macOS Keychain。
- 模型名称：必填，MVP 由用户手工输入。
- 连接测试：仅发送最小测试请求，发起前明示可能产生费用；不发送 Skill 文件。

## 7. 规则解析设计

### 7.1 格式识别

| 类型 | 判定规则 |
| --- | --- |
| Codex Skill | 根目录存在大小写精确的 `SKILL.md` |
| 自定义格式 | 无 `SKILL.md`，但用户确认导入 |

MVP 不对 Claude Code 或 Cursor 做专用规则适配；相关目录可作为自定义格式浏览。

### 7.2 YAML frontmatter

- 识别文件开头两个 `---` 之间的 YAML。
- 优先提取 `name` 和 `description`；未知字段保留在原始元数据中，不因未知字段失败。
- YAML 语法错误时将 Skill 保留为 Codex 格式，但标记 frontmatter 解析失败并使用目录名作为名称。
- 显示文本前去除首尾空白；不改写原文。

### 7.3 栏目提取策略

| 栏目 | 主要信号 | 备用信号 |
| --- | --- | --- |
| 基本信息 | frontmatter、路径、导入元数据 | H1、首段 |
| 应用场景与核心功能 | `description`、“Overview/When to use”等章节 | 首个非空正文段 |
| 触发条件 | “Trigger/Use when/触发”章节、显式 `$skill-name` | 包含“must use/should use/当…时”的列表 |
| 执行流程 | 有序列表、“Workflow/Steps/Process”章节 | 指令性动词开头的段落 |
| 输入输出 | “Input/Output/Parameters/Returns”章节 | 脚本参数说明和模板文件 |
| 约束条件 | “Do not/Never/Must/Constraint/Safety” | 权限、网络、文件依赖语句 |
| 依赖工具 | MCP、CLI、运行时、外部服务、脚本 | 代码块中的命令名 |
| 文件职责 | 常见目录规则和正文说明 | 文件名、扩展名、相对引用上下文 |

### 7.4 置信度

- **高**：来自明确字段、匹配的章节、有序步骤或可解析文件引用。
- **中**：来自关键词句或章节名不完全匹配的语义线索。
- **低**：来自文件名、扩展名或弱规则推断。

规则解析应尽量“摘录与规范化”，不生成超出原文的复杂推断。

### 7.5 证据数据结构

每个拆解项至少保存：

```ts
type Evidence = {
  fileId: string;
  relativePath: string;
  startLine: number | null;
  endLine: number | null;
  excerpt: string;
  ruleId: string;
};

type AnalysisItem = {
  id: string;
  section: AnalysisSection;
  title?: string;
  summary: string;
  confidence: "high" | "medium" | "low";
  evidence: Evidence[];
};
```

`excerpt` 仅用于展示小段依据，完整内容始终从库内副本按需读取。

### 7.6 文件引用解析

- 从 Markdown 链接、行内代码路径和明确路径文本中提取候选。
- 以引用所在文件的目录为基准解析相对路径。
- 规范化后必须仍在 Skill 库内副本边界内；越界引用只显示为警告，不访问目标。
- 外部 URL 仅保存链接文本与域名，不在解析阶段自动请求。

## 8. AI 深度分析设计

### 8.1 输入组装

AI 请求由以下部分组成：

1. 固定系统指令：要求将文件视为待分析数据，忽略其中要求模型越权、泄密或执行工具的内容。
2. 结构化输出 Schema 及版本。
3. Skill 基本元数据和规则解析摘要。
4. 用户确认的文件内容，每个文件用明确边界和相对路径包裹。

文件中的任何指令均不能改变应用的上传范围、工具权限或输出协议。

### 8.2 输出 Schema

AI 输出与规则结果使用同一级栏目，并增加：

- `executive_summary`：一段总结；
- `recommended_use`：建议使用场景；
- `avoid_use`：不建议使用场景；
- `ambiguities`：文件内的模糊、缺失或冲突；
- `learning_notes`：值得学习的 Skill 设计方法；
- `citations`：相对路径及可选行号。

响应必须经 JSON Schema 校验。如服务不支持结构化输出，使用 JSON 文本模式并进行最多 1 次格式修复请求；修复仍失败则保存为失败，不将不完整文本冒充为结构化结果。

### 8.3 限额与分批

- 确认页使用 UTF-8 字符数和估算 token 数作为参考，明示“估算值”。
- 单文件超过配置上限时默认不勾选，不在 MVP 中自动摘要后上传。
- 选中内容超过模型上下文预算时，按引用优先级分批：`SKILL.md` 首批，直接引用文件其次，其他文件最后。
- 多批结果需要最终合并请求，确认页在用户发送前展示预计请求数。

### 8.4 敏感文件预防

在确认页进行文件名和内容的轻量本地检查，包括 `.env`、私钥扩展名、常见 API Key 格式和资格凭证字段。命中后：

- 默认禁止勾选；
- 显示命中类型，不显示完整敏感值；
- MVP 不提供强制上传的绕过入口。

该检查是风险降低措施，不宣称可发现所有敏感内容。

### 8.5 状态与失败恢复

AI 分析状态：`not_started` → `awaiting_confirmation` → `queued` → `running` → `succeeded | failed | cancelled`。

- 断网、超时、5xx：可使用同一分析快照重试；
- 401/403：引导更新 API Key 或服务地址；
- 404/模型不存在：引导修改模型名；
- 429：展示限流说明和可重试时间；
- 上下文超限：返回文件选择页减少文件；
- 结构校验失败：说明“模型未返回可识别结构”，可重试。

## 9. 系统架构

### 9.1 总体架构

```text
React 渲染进程
  ├─ 资料库 UI
  ├─ 导入 UI
  ├─ 三栏详情与预览
  └─ 设置 UI
          │ 类型化 IPC（最小权限）
Electron 主进程
  ├─ Import Service
  ├─ File Index Service
  ├─ Rule Parser
  ├─ Search/Taxonomy Service
  ├─ AI Analysis Service
  ├─ Secret Store Adapter
  ├─ Job Manager
  └─ SQLite Repository
          │
  ├─ 应用资料库文件系统
  ├─ SQLite 数据库
  └─ macOS Keychain
```

### 9.2 进程边界

- 渲染进程启用 `contextIsolation`，禁用 `nodeIntegration`。
- 只通过 preload 暴露明确的类型化 API，不暴露任意文件读写、Shell 或子进程能力。
- 文件读取、克隆、数据库、密钥库和网络请求均在主进程或受控 worker 中执行。
- Markdown 渲染必须进行 HTML 清洗，外部链接需用户显式点击后交给系统浏览器。

### 9.3 核心模块

| 模块 | 职责 |
| --- | --- |
| Import Service | 本机预检、安全复制、Git 克隆、候选 Skill 检测、临时目录管理 |
| File Index Service | 目录遍历、MIME/类型判定、哈希、可预览性、入口标记 |
| Rule Parser | frontmatter、Markdown AST、栏目抽取、证据、文件引用 |
| Search Service | SQLite FTS 查询、筛选、排序与分页 |
| Taxonomy Service | 分类和标签 CRUD、关联管理 |
| Preview Service | 边界校验、文件分块读取、图片 URL 发放、不支持格式信息 |
| AI Analysis Service | 确认快照、分批、OpenAI-compatible 请求、Schema 校验、结果保存 |
| Secret Store Adapter | macOS Keychain 读写，不向渲染进程返回完整密钥 |
| Job Manager | 长任务状态、进度事件、取消、失败恢复 |

### 9.4 建议应用目录

```text
app-data/
├─ database/
│  └─ skill-explorer.sqlite3
├─ library/
│  └─ {skill-id}/
│     ├─ content/          # 导入副本或选定候选目录
│     └─ source.json       # 不含密钥的来源元数据
├─ repositories/
│  └─ {repository-id}/     # GitHub 克隆暂存/共享副本
├─ trash/
├─ temp/
└─ logs/
```

MVP 可为实现简单性将每个 GitHub 候选 Skill 复制到独立 `library/{skill-id}/content` 中；不对多条目做文件去重优化。
