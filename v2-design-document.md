# Skill 拆解器 v0.2 设计与验收说明

## 1. 设计结论

v0.2 将来源同步、在线编辑、历史恢复、安装与导出统一建模为“受控变更”：先在临时区构造候选内容，再生成文件级或逐行差异，只有用户确认后才写入。资料库内容变化会重新建立文件索引、重新运行规则拆解，并保存不可变的完整版本快照。

## 2. 功能边界

| 能力 | v0.2 行为 | 安全边界 |
| --- | --- | --- |
| 原始目录关联 | 本机 Skill 可检查现有来源或重新关联目录 | 仅读取来源，不反向写入 |
| GitHub 更新 | 手动浅克隆记录的分支，按导入时的 Skill 子目录生成差异 | 禁用 Hook、子模块和 LFS 下载，不运行仓库内容 |
| Skill 对比 | 比较两个 Skill 的八类规则拆解，分离共同点和各自特有项 | 不调用 AI |
| 在线编辑 | 支持不超过 1 MB 的可预览文本文件，保存前展示逐行 Diff | 仅编辑资料库副本 |
| 历史版本 | 初始导入及每次内容变更保存完整快照，可比较并恢复 | 恢复前保留新快照 |
| 复杂 Diff | 来源更新提供目录级增删改和文本逐行差异 | 二进制文件只展示哈希、大小与状态 |
| 引用图 | 将规则解析发现的已解析引用绘制为文件节点与有向边 | 点击节点只打开库内文件 |
| 安装/导出 | 先选择目标根目录并预览冲突，支持重命名或备份后覆盖 | 目标限定为所选根目录下的 Skill 子目录；不执行文件 |

## 3. 数据与迁移

- `skills` 新增 `source_subpath`、`source_branch`、`source_revision`、`source_checked_at`；旧数据库启动时增量迁移。
- 新增 `versions` 表，记录版本语义、来源、时间、文件数和快照路径。
- GitHub 导入记录提交 SHA、分支和候选子目录，后续更新不会错误同步整个仓库。
- 内容替换、规则分析替换与版本记录在同一个 SQLite 事务中提交；文件系统写入使用 next/previous 目录进行可回滚切换。

## 4. 验收清单

1. 本机来源文件变化后，检查页正确显示新增、修改、删除；取消不改变资料库。
2. GitHub Skill 可手动检查远端；应用更新后记录新提交 SHA。
3. 两个 Skill 可按八类栏目并排展示共同点和差异。
4. 文本编辑保存前展示新旧行号；确认后规则结果和文件哈希更新。
5. 历史页可列出版本、查看其与当前内容的文件差异并恢复。
6. 引用图只绘制解析结果中的库内文件，不访问外部链接。
7. 安装/导出必须先选择目标并显示冲突；覆盖时原目标先进入应用备份区。
8. `npm test`、`npm run typecheck`、`npm run lint`、`npm run build` 全部通过。
